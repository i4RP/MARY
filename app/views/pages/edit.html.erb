  <h1>イーサリアムアドレスの登録</h1>
  <!-- ここからフォーム -->
  <%= form_for @addressup do |f| %>
    <div class="field">
      <label>ご自身のイーサリアムアドレス</label><br>
      <%= f.text_field :address %>
    </div>
    <div class="actions">
      <%= f.submit "登録",class:"btn btn-success" %>
    </div>
  <% end %>
